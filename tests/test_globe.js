/**
 * Test prototype for generating high-definition procedural Earth texture for Three.js
 */
const fs = require('fs');

console.log('Testing Three.js Canvas Texture generator...');
// Verify Node environment
console.log('Node version:', process.version);
